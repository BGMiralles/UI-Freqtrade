import React from 'react';
import { NewStrategy } from '../../common/NewStrategy/NewStrategy';

export const ViewNewStrategy = () => {
    return (
        <div>
            <NewStrategy />
        </div>
    );
};
